/**
 * Main Application Module
 * Initializes the entire application on DOMContentLoaded
 */

// Note: setupEventListeners() is now called from components.js
// after components are fully loaded to ensure all DOM elements exist
